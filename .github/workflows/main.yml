 - name: Make Code Badge
	uses: shadowmoose/GHA-LoC-Badge@1.0.0
    id: loc_badge
    with:
      debug: true
      directory: ./
      badge: ./badges/badge.svg
      patterns: '*.zig|*.asm|*.S'  # Patterns in the format of a '.gitignore' file, separated by pipes.
